<%= form_for(project) do |f| %>
  <% if project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
      <div class="row">
    <%= f.label :title, class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :title, class: 'form-control', autofocus: true, required: true %>
      <br>
    </div>
  </div>
  </div>

  <div class="field">
    <div class="row">
    <%= f.label :company, class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :company, class: 'form-control', autofocus: true, required: true %>
      <br>
    </div>
  </div>
  </div>

  <div class="field">
    <div class="row">
    <%= f.label :project_date, class:'col-sm-2 control-label' %>
    <div class="col-sm-6">
      <div class='input-group date' id='datetimepicker7'>
      <%= f.text_field :project_date, class: "form-control", required: true %>
      <span class="input-group-addon">
          <span class="glyphicon glyphicon-calendar"></span>
      </span></div>
      <script type="text/javascript">
      $(function(){
        $('#datetimepicker7').datetimepicker({
            format: 'YYYY-MM-DD'
        });
      });
      </script>
      <br>
    </div>
  </div>
  </div>
  <div class="field">
    <div class="row">
    <%= f.label :description, class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_area :description, class: 'form-control', autofocus: true, maxlength: 400, placeholder: 'max: 400 words' %>
      <br>
    </div>
  </div>
  </div>

  <div class="field">
    <div class="col-sm-offset-2 col-sm-10">
      <div class="pull-right">
        <%= f.submit 'Save', class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
<script type='text/javascript'>
/*
$( document ).ready(function () {
  $( "#new_reference" ).validate( {
    rules: {
      title: {required: true},
      company: {required: true},
      project_date: {required: true}


    },
    messages: {
      title: "Title can't be blank",
      company: "Company can't be blank",
      project_date: "Date can't be blank"

    },
    errorElement: "em",
    errorPlacement: function ( error, element ) {
      // Add the `help-block` class to the error element
      error.addClass( "help-block" );

      if ( element.prop( "type" ) === "checkbox" ) {
        error.insertAfter( element.parent( "label" ) );
      } else {
        error.insertAfter( element );
      }

    },
    highlight: function ( element, errorClass, validClass ) {
      $( element ).parents( ".col-sm-10, .col-sm-6" ).addClass( "has-error" ).removeClass( "has-success" );
    },
    unhighlight: function (element, errorClass, validClass) {
      $( element ).parents( ".col-sm-10, .col-sm-6" ).addClass( "has-success" ).removeClass( "has-error" );
    }
  });
});
*/

</script>
