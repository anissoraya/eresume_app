<%= form_for(video_gallery) do |f| %>
  <% if video_gallery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_gallery.errors.count, "error") %> prohibited this video_gallery from being saved:</h2>

      <ul>
      <% video_gallery.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label 'Title', class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :name, class: 'form-control', autofocus: true %>
      <br>
    </div>
  </div>

  <div class="field">
    <%= f.label 'Youtube Link', class:'col-sm-2 control-label' %>
    <div class="col-sm-10">
      <%= f.text_field :link, class: 'form-control', autofocus: true do %>
        <span>http://www.youtube.com/embed</span>
      <%end%>
      <br>
    </div>
  </div>

  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <div class="pull-right">
        <%= f.submit 'Save', class: 'btn btn-primary' %>
      </div>
    </div>
  </div>
<% end %>
